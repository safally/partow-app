# مستندات فنی پروژه Partow (پرتو)

**نوع پروژه:** اپلیکیشن اندروید بازی تمرین تمرکز ذهنی، ساخته‌شده از یک وب‌اپ HTML/CSS/JS و بسته‌بندی‌شده با Capacitor
**Package ID:** `com.partow.app`
**مخزن کد:** https://github.com/safally/partow-app

---

## ۱. معرفی پروژه

Partow (پرتو) یک بازی تمرین تمرکز ذهنی است که کاربر باید در هر مرحله (کارت) بین چند حالت مختلف — رنگ، شکل، یا عدد — تشخیص «مطابقت دارد / مطابقت ندارد» بدهد. بازی شامل موارد زیر است:

- سیستم امتیاز، کمبو (Combo) و ضریب (Multiplier)
- حالت کالیبراسیون اولیه برای سنجش سطح کاربر
- سه حالت بازی: رنگ، شکل، عدد (و یک حالت ترکیبی)
- کارت‌های «تله» (خاکستری) که باید نادیده گرفته شوند
- داشبورد پیشرفت و رکورد شخصی (ذخیره در `localStorage`)
- تجربه‌ی آرام‌سازی (breathing exercise) بعد از پایان جلسه

هسته‌ی بازی یک فایل HTML مستقل است (`www/index.html`) که تمام منطق، استایل و صداها (از طریق Web Audio API) را در خودش دارد — بدون وابستگی به فریم‌ورک خارجی.

---

## ۲. پشته فناوری (Tech Stack)

| لایه | فناوری |
|---|---|
| هسته بازی (UI/منطق) | HTML5, CSS3, Vanilla JavaScript |
| صدا | Web Audio API (بدون فایل صوتی خارجی، تولید تن با نوسان‌ساز) |
| تبدیل به اپ اندروید | [CapacitorJS](https://capacitorjs.com/) |
| پروژه native اندروید | Java + Gradle (به‌صورت خودکار توسط Capacitor ساخته می‌شود) |
| مدیریت پکیج | npm |
| کنترل نسخه | Git / GitHub |

---

## ۳. ساختار پوشه‌ها

```
partow-app/
├── android/                  # پروژه native اندروید (تولیدشده توسط Capacitor)
│   └── app/
│       ├── src/main/AndroidManifest.xml
│       └── src/main/res/     # آیکون‌ها، اسپلش اسکرین، رنگ‌ها
├── www/
│   └── index.html            # کل بازی (HTML+CSS+JS) — نقطه ورود اصلی
├── capacitor.config.json     # تنظیمات Capacitor (appId، اسپلش اسکرین و...)
├── package.json
└── package-lock.json
```

> نکته: پوشه‌های `node_modules/` و `android/app/build/` به‌عمد از ردیابی Git خارج شده‌اند (در `.gitignore`) چون حجیم و قابل بازتولید هستند.

---

## ۴. پلاگین‌های Capacitor مورد استفاده

| پلاگین | کاربرد |
|---|---|
| `@capacitor/core`, `@capacitor/cli` | هسته Capacitor |
| `@capacitor/android` | پلتفرم اندروید |
| `@capacitor/splash-screen` | نمایش اسپلش اسکرین موقع اجرای اپ |
| `@capacitor/status-bar` | کنترل ظاهر و همپوشانی نوار وضعیت گوشی |
| `@capacitor/app` | مدیریت رویدادهای اپ (از جمله خروج کامل با `exitApp()`) |

تنظیمات فعلی اسپلش اسکرین در `capacitor.config.json`:

```json
{
  "appId": "com.partow.app",
  "appName": "Partow",
  "webDir": "www",
  "plugins": {
    "SplashScreen": {
      "launchShowDuration": 2000,
      "backgroundColor": "#07070f",
      "showSpinner": false
    }
  }
}
```

---

## ۵. راه‌اندازی محیط توسعه

```bash
# ۱. کلون کردن پروژه
git clone https://github.com/safally/partow-app.git
cd partow-app

# ۲. نصب وابستگی‌ها
npm install

# ۳. سینک کردن با پروژه اندروید
npx cap sync android

# ۴. باز کردن در اندروید استودیو
npx cap open android
```

---

## ۶. فرآیند ساخت نسخه نصبی (Build & Signing)

برای انتشار (حتی نسخه تست)، باید یک APK **امضاشده (Signed)** ساخته شود؛ APK دیباگ معمولی برای توزیع یا کافه‌بازار قابل قبول نیست.

1. در اندروید استودیو: `Build > Generate Signed Bundle / APK`
2. گزینه `APK` را انتخاب کنید (نه App Bundle)
3. یک Keystore بسازید یا Keystore موجود پروژه را انتخاب کنید
   - ⚠️ فایل Keystore و رمزهایش باید در جای امن نگهداری شوند؛ گم شدن آن یعنی امکان انتشار آپدیت با همان هویت امضا از بین می‌رود.
4. Build Variant: `release`
5. Signature Version: `V2 (Full APK Signature)`
6. خروجی نهایی در مسیر مشابه `android/app/release/app-release.apk` قرار می‌گیرد.

---

## ۷. تاریخچه رفع ایرادات (Changelog)

بازبینی فنی اولیه توسط تیم، ۵ ایراد را مشخص کرد که به شرح زیر بررسی و رفع شدند:

| # | ایراد | علت ریشه‌ای | راه‌حل |
|---|---|---|---|
| ۱ | کندی روی برخی گوشی‌های اندروید | صفحات غیرفعال (`panel-screen` بدون کلاس `active`) فقط با `opacity:0` مخفی می‌شدند؛ انیمیشن‌های CSS داخلشان (شناوری مسکات، پالس مغز و...) بی‌نهایت در پس‌زمینه اجرا می‌ماندند و منابع GPU/CPU مصرف می‌کردند. | افزودن قانون CSS که با `animation-play-state: paused` تمام انیمیشن‌های صفحات غیرفعال را متوقف می‌کند. |
| ۲ | نبود اسپلش اسکرین | کد از قبل منتظر `SplashScreen.hide()` بود، ولی پلاگین `@capacitor/splash-screen` نصب نشده بود. | نصب پلاگین + تنظیم مدت‌زمان و رنگ پس‌زمینه در `capacitor.config.json`. |
| ۳ | نوار وضعیت (status bar) بزرگ | کد از قبل `StatusBar.setOverlaysWebView` و `setStyle` را صدا می‌زد، ولی پلاگین `@capacitor/status-bar` نصب نبود، پس این تنظیمات هیچ اثری نداشتند. | نصب پلاگین `@capacitor/status-bar`. |
| ۴ | دکمه خروج کار نمی‌کرد | منطق دکمه از پیش صحیح بود و در نبود پلاگین، پیام «پلاگین خروج نصب نشده» را نشان می‌داد — یعنی خودش داشت علت را گزارش می‌داد. | نصب پلاگین `@capacitor/app` که متد `exitApp()` را فراهم می‌کند. |
| ۵ | استفاده از ایموجی به‌جای آیکون | دکمه‌های پاز (⏸)، صدا (🔊/🔇)، «مطابقت دارد» (✓) و «مطابقت ندارد» (✕) از کاراکترهای ایموجی/یونیکد استفاده می‌کردند که روی گوشی‌های مختلف ظاهر متفاوتی دارند. | جایگزینی هر چهار مورد با آیکون SVG سفارشی، هم‌راستا با سبک آیکون‌های دیگر پروژه (مثل آیکون رکورد/پیشرفت). |

پس از رفع موارد ۲ تا ۴، دستور زیر باید اجرا و سپس یک APK جدید ساخته شود:

```bash
npm install @capacitor/splash-screen @capacitor/status-bar @capacitor/app
npx cap sync android
```

---

## ۸. نکات باز / موارد قابل پیگیری بعدی

- تصویر اسپلش اسکرین فعلی، لوگوی پیش‌فرض Capacitor است؛ جایگزینی آن با هویت بصری بازی (مسکوت مغز یا لوگوی «پرتو») هنوز انجام نشده.
- عملکرد دکمه خروج (`exitApp`) باید روی چند مدل واقعی گوشی تست شود تا از رفتار یکسان مطمئن شویم.
- تست عملکرد نهایی (کندی رفع‌شده یا نه) باید مجدداً روی همان گوشی‌های ضعیف گزارش‌شده انجام شود.
